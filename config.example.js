/* ============================================
   ROAMIO — config.example.js
   ✅  This file IS committed to GitHub (no real keys).

   SETUP INSTRUCTIONS:
   1. Copy this file and rename it to config.js
   2. Replace the placeholder values with your real API keys
   3. config.js is in .gitignore so it will never be committed
   ============================================ */

window.GEMINI_API_KEY = 'YOUR_GEMINI_API_KEY_HERE';
window.UNSPLASH_KEY   = 'YOUR_UNSPLASH_KEY_HERE';
